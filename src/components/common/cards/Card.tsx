import Card, { CardProps, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '../../ui/Card';

export type { CardProps };
export { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter };
export default Card;
